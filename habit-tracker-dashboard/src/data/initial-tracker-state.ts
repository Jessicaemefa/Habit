import type { HabitState, TaskState } from "@/types/tracker";

/** Fresh install: zero habits and tasks — onboarding explains the flow */
export const DEFAULT_HABITS: HabitState[] = [];

export const DEFAULT_TASKS: TaskState[] = [];
